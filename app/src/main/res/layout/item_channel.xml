<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="6dp"
    android:paddingEnd="6dp"
    android:paddingTop="3dp"
    android:paddingBottom="3dp">

    <!-- Main channel row with glassmorphic background -->
    <LinearLayout
        android:id="@+id/channel_bg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@drawable/bg_channel_item"
        android:padding="14dp"
        android:gravity="center_vertical">

        <!-- Drag handle (only visible in reorder mode) -->
        <ImageView
            android:id="@+id/drag_handle"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@android:drawable/ic_menu_sort_by_size"
            android:layout_marginEnd="8dp"
            android:visibility="gone"
            android:contentDescription="Drag" />

        <!-- Channel type icon with skeuomorphic emboss -->
        <ImageView
            android:id="@+id/channel_icon"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@android:drawable/ic_menu_edit"
            android:layout_marginEnd="10dp" />

        <!-- Channel name -->
        <TextView
            android:id="@+id/channel_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="general"
            android:textColor="#2C2C2C"
            android:textSize="15sp"
            android:textStyle="bold"
            android:shadowColor="#44FFFFFF"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="1" />

        <!-- Voice member count badge -->
        <TextView
            android:id="@+id/channel_voice_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#2E7D32"
            android:textSize="13sp"
            android:textStyle="bold"
            android:background="#22558B2F"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Connected voice users (expandable) -->
    <LinearLayout
        android:id="@+id/voice_members_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="48dp"
        android:paddingEnd="12dp"
        android:paddingBottom="4dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/voice_members_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#555555"
            android:textSize="13sp"
            android:lineSpacingExtra="2dp"
            android:paddingTop="2dp" />
    </LinearLayout>

</LinearLayout>
